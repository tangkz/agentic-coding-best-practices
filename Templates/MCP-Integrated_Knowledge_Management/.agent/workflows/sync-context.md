---
description: Synchronize project context with external sources
---

// turbo
1. Run the `knowledge-retriever` skill or an `execution/` script to fetch live context.
2. Update the `directives/context.md` with verified data.
3. Compare against previous state in `.tmp/`.
4. Update `directives/` SOPs if the external environment has changed.
