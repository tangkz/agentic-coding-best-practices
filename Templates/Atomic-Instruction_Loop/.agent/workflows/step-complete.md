---
description: Mark a step as complete and prepare for the next
---

// turbo
1. Run `execution/verify_step.py` to ensure the change meets the directive.
2. Update `directives/task_list.md` with completion status.
3. Stage the change using a git script in `execution/`.
4. Wait for the human to signal the next atomic step.
