---
description: Perform a visual "vibe check" using the browser
---

// turbo
1. Start the dev server using a script in `execution/`.
2. Use the browser subagent to evaluate the UI.
3. Save visual artifacts to `.tmp/`.
4. Update `directives/` if visual standards need adjustment.
